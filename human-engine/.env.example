# Try-on provider selection. The body engine works with this unset.
TRYON_PROVIDER=mock

# Only read by the Vertex provider. Credentials come from ADC / IAM, never source.
VERTEX_PROJECT_ID=
VERTEX_LOCATION=us-central1
VERTEX_TRYON_MODEL=
