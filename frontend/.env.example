BACKEND_URL=http://127.0.0.1:8000
# Leave empty to call the Next.js rewrite proxy (/v1 → BACKEND_URL).
# Set only if the browser should call the API origin directly (requires CORS on the API).
NEXT_PUBLIC_API_BASE_URL=
